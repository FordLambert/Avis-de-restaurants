import React from 'react';

//Loading component, in case the map don't load
const Pulser = () => (
    <div className='pulser-wrapper'>
        <div className='pulse-1 rounded-circle'/>
        <div className='pulse-2 rounded-circle'/>
    </div>
);

export default Pulser;